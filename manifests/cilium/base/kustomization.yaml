---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kube-system

helmGlobals:
  chartHome: ../../../_out/charts/

helmCharts:
  - name: cilium
    repo: https://helm.cilium.io/
    version: 1.15.6

    namespace: kube-system
    releaseName: cilium

    includeCRDs: true
    valuesFile: values.yaml

resources:
  # Need to wait for the CRDs to settle before we can apply these policies,
  # we'll apply them during bootstrap
  #- ../policies/hubble-relay-policy.yaml
  #- ../policies/permitted-cluster-dns.yaml
